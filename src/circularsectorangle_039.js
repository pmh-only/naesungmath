/**
 * 039 부채꼴의 중심각 표시_rad
 * 
 * @author: Naesung
 * @param {number} r
 * @param {number} l
 * @return {number} The center angle of sector form (radian)
 * @since <0.37.0
 */
exports.circularSectorAngle = function (r, l) {
    return (l / r)
}
