/**
 * 019 마름모의 넓이 공식
 * 
 * @author: Naesung
 * @param {number} a
 * @param {number} b
 * @return {number} The area of rhombus with digonal a, b
 * @since <0.37.0
 */
exports.rhombusArea = function (a, b) {
    return (0.5 * (a) * (b))
}
