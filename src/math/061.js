/**
   * 061 ������ü�� �ѳ��� ����
   *
   * @author: Choi Minseo
   * @param {number} a
   * @param {number} b
   * @param {number} c
   * @return {number} The surface area of cuboid
   *
   */
export function cuboidArea (a, b, c) {
  return (2 * ((a * b) + (b * c) + (c * a)))
}
