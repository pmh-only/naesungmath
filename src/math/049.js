/**
 * 049 ������ ����
 *
 * @author: Choi Minseo
 * @param {number} r
 * @param {number} h
 * @return {number} The volume of cone
 *
 */
export function coneVolume (r, h) {
  return ((1 / 3) * (Math.PI) * (r ** 2) * h)
}
