/*
const nmath = require('./src')
const assert = require('assert')
const repl = require('repl')

const help = 'naesungmath repl 서버'

const r = repl.start('> ')
r.context.help = help
r.context.nmath = nmath
r.context.assert = assert
*/
